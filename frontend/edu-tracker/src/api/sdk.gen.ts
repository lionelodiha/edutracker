// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AcceptOrganizationInviteEndpointHandlerData, AcceptOrganizationInviteEndpointHandlerErrors, AcceptOrganizationInviteEndpointHandlerResponses, CancelOrganizationInviteEndpointHandlerData, CancelOrganizationInviteEndpointHandlerErrors, CancelOrganizationInviteEndpointHandlerResponses, CreateCourseEndpointHandlerData, CreateCourseEndpointHandlerErrors, CreateCourseEndpointHandlerResponses, CreateCourseOfferingEndpointHandlerData, CreateCourseOfferingEndpointHandlerErrors, CreateCourseOfferingEndpointHandlerResponses, CreateOrganizationEndpointHandlerData, CreateOrganizationEndpointHandlerErrors, CreateOrganizationEndpointHandlerResponses, CreateSemesterEndpointHandlerData, CreateSemesterEndpointHandlerErrors, CreateSemesterEndpointHandlerResponses, CreateTermEndpointHandlerData, CreateTermEndpointHandlerErrors, CreateTermEndpointHandlerResponses, DeleteCourseEndpointHandlerData, DeleteCourseEndpointHandlerErrors, DeleteCourseEndpointHandlerResponses, DeleteCourseOfferingEndpointHandlerData, DeleteCourseOfferingEndpointHandlerErrors, DeleteCourseOfferingEndpointHandlerResponses, DeleteOrganizationEndpointHandlerData, DeleteOrganizationEndpointHandlerErrors, DeleteOrganizationEndpointHandlerResponses, DeleteSemesterEndpointHandlerData, DeleteSemesterEndpointHandlerErrors, DeleteSemesterEndpointHandlerResponses, DeleteTermEndpointHandlerData, DeleteTermEndpointHandlerErrors, DeleteTermEndpointHandlerResponses, DemoteUserEndpointHandlerData, DemoteUserEndpointHandlerErrors, DemoteUserEndpointHandlerResponses, GetCourseByIdEndpointHandlerData, GetCourseByIdEndpointHandlerErrors, GetCourseByIdEndpointHandlerResponses, GetCourseOfferingsBySemesterEndpointHandlerData, GetCourseOfferingsBySemesterEndpointHandlerErrors, GetCourseOfferingsBySemesterEndpointHandlerResponses, GetCoursesEndpointHandlerData, GetCoursesEndpointHandlerErrors, GetCoursesEndpointHandlerResponses, GetCurrentUserEndpointHandlerData, GetCurrentUserEndpointHandlerErrors, GetCurrentUserEndpointHandlerResponses, GetCurrentUserSessionsEndpointHandlerData, GetCurrentUserSessionsEndpointHandlerErrors, GetCurrentUserSessionsEndpointHandlerResponses, GetOrganizationByIdEndpointHandlerData, GetOrganizationByIdEndpointHandlerErrors, GetOrganizationByIdEndpointHandlerResponses, GetOrganizationInvitesEndpointHandlerData, GetOrganizationInvitesEndpointHandlerErrors, GetOrganizationInvitesEndpointHandlerResponses, GetOrganizationMembersEndpointHandlerData, GetOrganizationMembersEndpointHandlerErrors, GetOrganizationMembersEndpointHandlerResponses, GetOrganizationsEndpointHandlerData, GetOrganizationsEndpointHandlerErrors, GetOrganizationsEndpointHandlerResponses, GetSemesterByIdEndpointHandlerData, GetSemesterByIdEndpointHandlerErrors, GetSemesterByIdEndpointHandlerResponses, GetSemestersEndpointHandlerData, GetSemestersEndpointHandlerErrors, GetSemestersEndpointHandlerResponses, GetTermByIdEndpointHandlerData, GetTermByIdEndpointHandlerErrors, GetTermByIdEndpointHandlerResponses, GetTermsBySemesterEndpointHandlerData, GetTermsBySemesterEndpointHandlerErrors, GetTermsBySemesterEndpointHandlerResponses, GetUserByIdEndpointHandlerData, GetUserByIdEndpointHandlerErrors, GetUserByIdEndpointHandlerResponses, GetUserInvitesEndpointHandlerData, GetUserInvitesEndpointHandlerErrors, GetUserInvitesEndpointHandlerResponses, GetUsersEndpointHandlerData, GetUsersEndpointHandlerErrors, GetUsersEndpointHandlerResponses, InviteOrganizationMemberEndpointHandlerData, InviteOrganizationMemberEndpointHandlerErrors, InviteOrganizationMemberEndpointHandlerResponses, LockUserEndpointHandlerData, LockUserEndpointHandlerErrors, LockUserEndpointHandlerResponses, LoginUserEndpointHandlerData, LoginUserEndpointHandlerErrors, LoginUserEndpointHandlerResponses, LogoutUserEndpointHandlerData, LogoutUserEndpointHandlerErrors, LogoutUserEndpointHandlerResponses, PromoteUserEndpointHandlerData, PromoteUserEndpointHandlerErrors, PromoteUserEndpointHandlerResponses, RefreshSessionEndpointHandlerData, RefreshSessionEndpointHandlerErrors, RefreshSessionEndpointHandlerResponses, RegisterUserEndpointHandlerData, RegisterUserEndpointHandlerErrors, RegisterUserEndpointHandlerResponses, RejectOrganizationInviteEndpointHandlerData, RejectOrganizationInviteEndpointHandlerErrors, RejectOrganizationInviteEndpointHandlerResponses, RemoveOrganizationMemberEndpointHandlerData, RemoveOrganizationMemberEndpointHandlerErrors, RemoveOrganizationMemberEndpointHandlerResponses, RevokeAllCurrentUserSessionsEndpointHandlerData, RevokeAllCurrentUserSessionsEndpointHandlerErrors, RevokeAllCurrentUserSessionsEndpointHandlerResponses, RevokeCurrentUserSessionEndpointHandlerData, RevokeCurrentUserSessionEndpointHandlerErrors, RevokeCurrentUserSessionEndpointHandlerResponses, TransferOrganizationOwnershipEndpointHandlerData, TransferOrganizationOwnershipEndpointHandlerErrors, TransferOrganizationOwnershipEndpointHandlerResponses, UnlockUserEndpointHandlerData, UnlockUserEndpointHandlerErrors, UnlockUserEndpointHandlerResponses, UpdateCourseEndpointHandlerData, UpdateCourseEndpointHandlerErrors, UpdateCourseEndpointHandlerResponses, UpdateCurrentUserEndpointHandlerData, UpdateCurrentUserEndpointHandlerErrors, UpdateCurrentUserEndpointHandlerResponses, UpdateCurrentUserPasswordEndpointHandlerData, UpdateCurrentUserPasswordEndpointHandlerErrors, UpdateCurrentUserPasswordEndpointHandlerResponses, UpdateOrganizationEndpointHandlerData, UpdateOrganizationEndpointHandlerErrors, UpdateOrganizationEndpointHandlerResponses, UpdateOrganizationMemberRoleEndpointHandlerData, UpdateOrganizationMemberRoleEndpointHandlerErrors, UpdateOrganizationMemberRoleEndpointHandlerResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Register a new user
 *
 * Registers a new user with the provided details:
 *
 * - `UserName`: Desired username (trimmed).
 * - `Email`: User's email (hashed internally).
 * - `Password`: User's password (hashed internally).
 * - `FirstName`, `MiddleName`, `LastName`: Personal names (encrypted).
 *
 * On success, the `Location` header contains the URI of the new user.
 *
 * Possible responses:
 * - `201 Created`: User successfully registered.
 * - `400 BadRequest`: Invalid or missing input.
 * - `409 Conflict`: Email already in use.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const registerUserEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<RegisterUserEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<RegisterUserEndpointHandlerResponses, RegisterUserEndpointHandlerErrors, ThrowOnError>({
    url: '/api/auth/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Login a user
 *
 * Logs in a user and creates a new session:
 *
 * - `Identifier`: Email or username.
 * - `Password`: User's password.
 * - `RememberMe`: Optional; if true, session lasts 7 days, otherwise 8 hours.
 *
 * On success, a secure HTTP-only cookie `edu_session_id` is set to manage the session.
 *
 * Possible responses:
 * - `200 OK`: Login successful.
 * - `400 BadRequest`: Invalid input.
 * - `401 Unauthorized`: Incorrect credentials or account locked.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const loginUserEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<LoginUserEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<LoginUserEndpointHandlerResponses, LoginUserEndpointHandlerErrors, ThrowOnError>({
    url: '/api/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refresh user session
 *
 * Refreshes the current user session, extending its expiration
 * **only if the session is within the allowed refresh threshold**.
 *
 * **Authentication Required**: A valid session is needed.
 *
 * On success, a new secure HTTP-only `edu_session_id` cookie is set with updated timestamps.
 *
 * Possible responses:
 * - `200 OK`: Session successfully refreshed.
 * - `401 Unauthorized`: No valid session, session expired, or not eligible for refresh yet.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const refreshSessionEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<RefreshSessionEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).post<RefreshSessionEndpointHandlerResponses, RefreshSessionEndpointHandlerErrors, ThrowOnError>({ url: '/api/auth/refresh', ...options });

/**
 * Logout user
 *
 * Logs out the current user by revoking their session.
 *
 * **Authentication Required**: A valid session is needed.
 *
 * This endpoint performs the following actions:
 * - Revokes the current session in the database.
 * - Deletes the `edu_session_id` cookie from the client.
 *
 * Possible responses:
 * - `200 OK`: Successfully logged out.
 * - `401 Unauthorized`: No valid session (user may already be logged out).
 * - `500 InternalServerError`: Unexpected server error.
 */
export const logoutUserEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<LogoutUserEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).post<LogoutUserEndpointHandlerResponses, LogoutUserEndpointHandlerErrors, ThrowOnError>({ url: '/api/auth/logout', ...options });

/**
 * List courses
 *
 * Retrieves all courses for an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Any active organization member can view courses.
 *
 * Possible responses:
 * - `200 OK`: Courses retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 */
export const getCoursesEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetCoursesEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetCoursesEndpointHandlerResponses, GetCoursesEndpointHandlerErrors, ThrowOnError>({ url: '/api/courses', ...options });

/**
 * Create course
 *
 * Creates a new course for an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Request Body**:
 * - `organizationId` (uuid, required): Organization identifier.
 * - `name` (string, required): Course name.
 * - `code` (string, required): Course code.
 *
 * **Access**:
 * - Only organization owners and moderators can create courses.
 *
 * Possible responses:
 * - `201 Created`: Course created successfully.
 * - `400 BadRequest`: Request body is invalid.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `409 Conflict`: A course with the same code already exists.
 */
export const createCourseEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<CreateCourseEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<CreateCourseEndpointHandlerResponses, CreateCourseEndpointHandlerErrors, ThrowOnError>({
    url: '/api/courses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete course
 *
 * Deletes a course from an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Course identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Only organization owners and moderators can delete courses.
 *
 * Possible responses:
 * - `200 OK`: Course deleted successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `404 NotFound`: Course was not found in the organization.
 */
export const deleteCourseEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<DeleteCourseEndpointHandlerData, ThrowOnError>) => (options.client ?? client).delete<DeleteCourseEndpointHandlerResponses, DeleteCourseEndpointHandlerErrors, ThrowOnError>({ url: '/api/courses/{id}', ...options });

/**
 * Get course by id
 *
 * Retrieves a single course by ID for an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Course identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Any active organization member can view the course.
 *
 * Possible responses:
 * - `200 OK`: Course retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 * - `404 NotFound`: Course was not found in the organization.
 */
export const getCourseByIdEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetCourseByIdEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetCourseByIdEndpointHandlerResponses, GetCourseByIdEndpointHandlerErrors, ThrowOnError>({ url: '/api/courses/{id}', ...options });

/**
 * Update course
 *
 * Updates an existing course.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Course identifier.
 *
 * **Request Body**:
 * - `organizationId` (uuid, required): Organization identifier.
 * - `name` (string, required): Updated course name.
 * - `code` (string, required): Updated course code.
 *
 * **Access**:
 * - Only organization owners and moderators can update courses.
 *
 * Possible responses:
 * - `200 OK`: Course updated successfully.
 * - `400 BadRequest`: Request body is invalid.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `404 NotFound`: Course was not found in the organization.
 * - `409 Conflict`: Another course already uses the requested code.
 */
export const updateCourseEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<UpdateCourseEndpointHandlerData, ThrowOnError>) => (options.client ?? client).patch<UpdateCourseEndpointHandlerResponses, UpdateCourseEndpointHandlerErrors, ThrowOnError>({
    url: '/api/courses/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create course offering
 *
 * Creates a course offering by linking a course to a term.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Request Body**:
 * - `organizationId` (uuid, required): Organization identifier.
 * - `courseId` (uuid, required): Course identifier.
 * - `termId` (uuid, required): Term identifier.
 *
 * **Access**:
 * - Only organization owners and moderators can create course offerings.
 *
 * Possible responses:
 * - `201 Created`: Course offering created successfully.
 * - `400 BadRequest`: Request body is invalid.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `404 NotFound`: Course or term was not found in the organization.
 * - `409 Conflict`: Course offering already exists or the entities do not belong to the same organization.
 */
export const createCourseOfferingEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<CreateCourseOfferingEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<CreateCourseOfferingEndpointHandlerResponses, CreateCourseOfferingEndpointHandlerErrors, ThrowOnError>({
    url: '/api/course-offerings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List course offerings by semester
 *
 * Retrieves course offerings for a semester.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `semesterId` (uuid): Semester identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Any active organization member can view course offerings.
 *
 * Possible responses:
 * - `200 OK`: Course offerings retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 * - `404 NotFound`: Semester was not found in the organization.
 */
export const getCourseOfferingsBySemesterEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetCourseOfferingsBySemesterEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetCourseOfferingsBySemesterEndpointHandlerResponses, GetCourseOfferingsBySemesterEndpointHandlerErrors, ThrowOnError>({ url: '/api/course-offerings/semester/{semesterId}', ...options });

/**
 * Delete course offering
 *
 * Deletes a course offering from an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Course offering identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Only organization owners and moderators can delete course offerings.
 *
 * Possible responses:
 * - `200 OK`: Course offering deleted successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `404 NotFound`: Course offering was not found in the organization.
 */
export const deleteCourseOfferingEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<DeleteCourseOfferingEndpointHandlerData, ThrowOnError>) => (options.client ?? client).delete<DeleteCourseOfferingEndpointHandlerResponses, DeleteCourseOfferingEndpointHandlerErrors, ThrowOnError>({ url: '/api/course-offerings/{id}', ...options });

/**
 * List organizations
 *
 * Retrieves organizations the current user belongs to.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * The response includes:
 * - `OrganizationId`, `Name`
 * - `Role`, `Status`
 *
 * Possible responses:
 * - `200 OK`: Organizations retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getOrganizationsEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<GetOrganizationsEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).get<GetOrganizationsEndpointHandlerResponses, GetOrganizationsEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations', ...options });

/**
 * Create organization
 *
 * Creates a new organization owned by the current user.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Request Body**:
 * - `Name` (string, required): Organization name.
 *
 * Possible responses:
 * - `201 Created`: Organization created successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const createOrganizationEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<CreateOrganizationEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<CreateOrganizationEndpointHandlerResponses, CreateOrganizationEndpointHandlerErrors, ThrowOnError>({
    url: '/api/organizations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete organization
 *
 * Deletes an organization. Only the owner can delete an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 *
 * Possible responses:
 * - `200 OK`: Organization deleted successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not the organization owner.
 * - `404 NotFound`: Organization not found.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const deleteOrganizationEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<DeleteOrganizationEndpointHandlerData, ThrowOnError>) => (options.client ?? client).delete<DeleteOrganizationEndpointHandlerResponses, DeleteOrganizationEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/{id}', ...options });

/**
 * Get organization by id
 *
 * Retrieves organization details by ID. Only active members can access this endpoint.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 *
 * Possible responses:
 * - `200 OK`: Organization retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 * - `404 NotFound`: Organization not found.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getOrganizationByIdEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationByIdEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationByIdEndpointHandlerResponses, GetOrganizationByIdEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/{id}', ...options });

/**
 * Update organization
 *
 * Updates an organization name. Only the owner can update the organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 *
 * **Request Body**:
 * - `Name` (string, required): New organization name.
 *
 * Possible responses:
 * - `200 OK`: Organization updated successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not the organization owner.
 * - `404 NotFound`: Organization not found.
 * - `409 Conflict`: Organization is locked.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const updateOrganizationEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<UpdateOrganizationEndpointHandlerData, ThrowOnError>) => (options.client ?? client).patch<UpdateOrganizationEndpointHandlerResponses, UpdateOrganizationEndpointHandlerErrors, ThrowOnError>({
    url: '/api/organizations/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get organization invites
 *
 * Retrieves pending invites for the organization. Only owners and moderators can access.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 *
 * Possible responses:
 * - `200 OK`: Invites retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to view invites.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getOrganizationInvitesEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationInvitesEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationInvitesEndpointHandlerResponses, GetOrganizationInvitesEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/{id}/invites', ...options });

/**
 * Invite organization member
 *
 * Sends an invitation to a user to join the organization. Only owners and moderators can invite.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 *
 * **Request Body**:
 * - `UserId` (uuid, required): User to invite.
 *
 * Possible responses:
 * - `200 OK`: Invite created successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to invite.
 * - `404 NotFound`: Organization or user not found.
 * - `409 Conflict`: User is already a member or an invite already exists.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const inviteOrganizationMemberEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<InviteOrganizationMemberEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<InviteOrganizationMemberEndpointHandlerResponses, InviteOrganizationMemberEndpointHandlerErrors, ThrowOnError>({
    url: '/api/organizations/{id}/invite',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Accept organization invite
 *
 * Accepts a pending organization invite for the current user.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `inviteId` (uuid): Invite identifier.
 *
 * Possible responses:
 * - `200 OK`: Invite accepted and membership created.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `404 NotFound`: Invite not found.
 * - `409 Conflict`: Invite expired or already responded to, or user is already a member.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const acceptOrganizationInviteEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<AcceptOrganizationInviteEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<AcceptOrganizationInviteEndpointHandlerResponses, AcceptOrganizationInviteEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/invites/{inviteId}/accept', ...options });

/**
 * Reject organization invite
 *
 * Rejects a pending organization invite for the current user.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `inviteId` (uuid): Invite identifier.
 *
 * Possible responses:
 * - `200 OK`: Invite rejected successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `404 NotFound`: Invite not found.
 * - `409 Conflict`: Invite expired or already responded to.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const rejectOrganizationInviteEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<RejectOrganizationInviteEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<RejectOrganizationInviteEndpointHandlerResponses, RejectOrganizationInviteEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/invites/{inviteId}/reject', ...options });

/**
 * Cancel organization invite
 *
 * Cancels a pending organization invite. Only owners and moderators can cancel invites.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 * - `inviteId` (uuid): Invite identifier.
 *
 * Possible responses:
 * - `200 OK`: Invite cancelled successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to cancel invites.
 * - `404 NotFound`: Invite not found.
 * - `409 Conflict`: Invite expired or already responded to.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const cancelOrganizationInviteEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<CancelOrganizationInviteEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<CancelOrganizationInviteEndpointHandlerResponses, CancelOrganizationInviteEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/{id}/invites/{inviteId}/cancel', ...options });

/**
 * Get user invites
 *
 * Retrieves pending organization invites for the current user.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * Possible responses:
 * - `200 OK`: Invites retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getUserInvitesEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<GetUserInvitesEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).get<GetUserInvitesEndpointHandlerResponses, GetUserInvitesEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/invites', ...options });

/**
 * Transfer organization ownership
 *
 * Transfers organization ownership to another active member.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 *
 * **Request Body**:
 * - `MemberId` (uuid, required): Target member ID.
 *
 * Possible responses:
 * - `200 OK`: Ownership transferred successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not the organization owner.
 * - `404 NotFound`: Organization or member not found.
 * - `409 Conflict`: Target member is already the owner.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const transferOrganizationOwnershipEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<TransferOrganizationOwnershipEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<TransferOrganizationOwnershipEndpointHandlerResponses, TransferOrganizationOwnershipEndpointHandlerErrors, ThrowOnError>({
    url: '/api/organizations/{id}/transfer-ownership',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update organization member role
 *
 * Updates a member's role within the organization. Only owners can update roles.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 * - `memberId` (uuid): Target member identifier.
 *
 * **Request Body**:
 * - `Role` (string, required): New role (Member, Moderator).
 *
 * Possible responses:
 * - `200 OK`: Role updated successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not the organization owner.
 * - `404 NotFound`: Organization member not found.
 * - `409 Conflict`: Owner role can only be transferred.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const updateOrganizationMemberRoleEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<UpdateOrganizationMemberRoleEndpointHandlerData, ThrowOnError>) => (options.client ?? client).patch<UpdateOrganizationMemberRoleEndpointHandlerResponses, UpdateOrganizationMemberRoleEndpointHandlerErrors, ThrowOnError>({
    url: '/api/organizations/{id}/members/{memberId}/role',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get organization members
 *
 * Retrieves members of the organization with their roles and names. Only active members can access.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 *
 * Possible responses:
 * - `200 OK`: Organization members retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getOrganizationMembersEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationMembersEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationMembersEndpointHandlerResponses, GetOrganizationMembersEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/{id}/members', ...options });

/**
 * Remove or leave organization member
 *
 * Removes a member from the organization, or allows a member to leave.
 * - If `memberId` is the current user, the request is treated as a leave action.
 * - Owners cannot leave; ownership must be transferred first.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Organization identifier.
 * - `memberId` (uuid): Member identifier to remove or leave.
 *
 * Possible responses:
 * - `200 OK`: Member removed successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: Insufficient permissions to remove the member.
 * - `404 NotFound`: Organization member not found.
 * - `409 Conflict`: Owner cannot be removed or removed from the organization.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const removeOrganizationMemberEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<RemoveOrganizationMemberEndpointHandlerData, ThrowOnError>) => (options.client ?? client).delete<RemoveOrganizationMemberEndpointHandlerResponses, RemoveOrganizationMemberEndpointHandlerErrors, ThrowOnError>({ url: '/api/organizations/{id}/members/{memberId}', ...options });

/**
 * List semesters
 *
 * Retrieves semesters for an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Any active organization member can view semesters.
 *
 * Possible responses:
 * - `200 OK`: Semesters retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 */
export const getSemestersEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetSemestersEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetSemestersEndpointHandlerResponses, GetSemestersEndpointHandlerErrors, ThrowOnError>({ url: '/api/semesters', ...options });

/**
 * Create semester
 *
 * Creates a new semester for an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Request Body**:
 * - `organizationId` (uuid, required): Organization identifier.
 * - `startYear` (number, required): Semester start year.
 *
 * **Access**:
 * - Only organization owners and moderators can create semesters.
 *
 * Possible responses:
 * - `201 Created`: Semester created successfully.
 * - `400 BadRequest`: Request body is invalid.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `409 Conflict`: Semester already exists for the given start year.
 */
export const createSemesterEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<CreateSemesterEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<CreateSemesterEndpointHandlerResponses, CreateSemesterEndpointHandlerErrors, ThrowOnError>({
    url: '/api/semesters',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete semester
 *
 * Deletes a semester from an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Semester identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Only organization owners and moderators can delete semesters.
 *
 * Possible responses:
 * - `200 OK`: Semester deleted successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `404 NotFound`: Semester was not found in the organization.
 */
export const deleteSemesterEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<DeleteSemesterEndpointHandlerData, ThrowOnError>) => (options.client ?? client).delete<DeleteSemesterEndpointHandlerResponses, DeleteSemesterEndpointHandlerErrors, ThrowOnError>({ url: '/api/semesters/{id}', ...options });

/**
 * Get semester by id
 *
 * Retrieves a semester by ID for an organization.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Semester identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Any active organization member can view semesters.
 *
 * Possible responses:
 * - `200 OK`: Semester retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 * - `404 NotFound`: Semester was not found in the organization.
 */
export const getSemesterByIdEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetSemesterByIdEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetSemesterByIdEndpointHandlerResponses, GetSemesterByIdEndpointHandlerErrors, ThrowOnError>({ url: '/api/semesters/{id}', ...options });

/**
 * Get current user sessions
 *
 * Retrieves the current user's active sessions.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Notes**:
 * - Revoked sessions are not included in the response.
 *
 * Possible responses:
 * - `200 OK`: Sessions retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getCurrentUserSessionsEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentUserSessionsEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentUserSessionsEndpointHandlerResponses, GetCurrentUserSessionsEndpointHandlerErrors, ThrowOnError>({ url: '/api/sessions/me', ...options });

/**
 * Revoke a current user specific session
 *
 * Revokes a specific session belonging to the current user.
 *
 * **Authentication Required**: A valid session is needed.
 *
 * **Route Parameter**:
 * - `id` (uuid): The ID of the session to revoke.
 *
 * **Behavior**:
 * - The specified session will be invalidated in the database.
 * - If the revoked session is the current session, the user may be logged out.
 * - No other sessions are affected.
 *
 * **Possible responses**:
 * - `200 OK`: Session successfully revoked.
 * - `404 NotFound`: Session ID does not exist or does not belong to the user.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const revokeCurrentUserSessionEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<RevokeCurrentUserSessionEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<RevokeCurrentUserSessionEndpointHandlerResponses, RevokeCurrentUserSessionEndpointHandlerErrors, ThrowOnError>({ url: '/api/sessions/{id}/revoke', ...options });

/**
 * Revoke all current user sessions
 *
 * Revokes all sessions for the current user **except the current one**.
 *
 * **Authentication Required**: A valid session is needed.
 *
 * **Query Parameter**:
 * - `keepCurrentUserSession` (bool, optional): If `true`, the current session will be preserved; default is `false`.
 *
 * **Behavior**:
 * - All sessions except the one specified (or current session if `keepCurrentUserSession = true`) are revoked.
 * - Any revoked session will be logged out automatically.
 *
 * **Possible responses**:
 * - `200 OK`: All targeted sessions successfully revoked.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const revokeAllCurrentUserSessionsEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<RevokeAllCurrentUserSessionsEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).post<RevokeAllCurrentUserSessionsEndpointHandlerResponses, RevokeAllCurrentUserSessionsEndpointHandlerErrors, ThrowOnError>({ url: '/api/sessions/revoke-all', ...options });

/**
 * Create term
 *
 * Creates a term inside a semester.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Request Body**:
 * - `organizationId` (uuid, required): Organization identifier.
 * - `semesterId` (uuid, required): Semester identifier.
 * - `ordinal` (number, required): Term position within the semester.
 *
 * **Access**:
 * - Only organization owners and moderators can create terms.
 *
 * Possible responses:
 * - `201 Created`: Term created successfully.
 * - `400 BadRequest`: Request body is invalid.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `404 NotFound`: Semester was not found in the organization.
 * - `409 Conflict`: Term already exists for the semester and ordinal.
 */
export const createTermEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<CreateTermEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<CreateTermEndpointHandlerResponses, CreateTermEndpointHandlerErrors, ThrowOnError>({
    url: '/api/terms',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List terms by semester
 *
 * Retrieves all terms for a semester.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `semesterId` (uuid): Semester identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Any active organization member can view terms.
 *
 * Possible responses:
 * - `200 OK`: Terms retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 * - `404 NotFound`: Semester was not found in the organization.
 */
export const getTermsBySemesterEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetTermsBySemesterEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetTermsBySemesterEndpointHandlerResponses, GetTermsBySemesterEndpointHandlerErrors, ThrowOnError>({ url: '/api/terms/semester/{semesterId}', ...options });

/**
 * Delete term
 *
 * Deletes a term from a semester.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Term identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Only organization owners and moderators can delete terms.
 *
 * Possible responses:
 * - `200 OK`: Term deleted successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not allowed to manage academics for the organization.
 * - `404 NotFound`: Term was not found in the organization.
 */
export const deleteTermEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<DeleteTermEndpointHandlerData, ThrowOnError>) => (options.client ?? client).delete<DeleteTermEndpointHandlerResponses, DeleteTermEndpointHandlerErrors, ThrowOnError>({ url: '/api/terms/{id}', ...options });

/**
 * Get term by id
 *
 * Retrieves a term by ID.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): Term identifier.
 *
 * **Query Parameters**:
 * - `organizationId` (uuid, required): Organization identifier.
 *
 * **Access**:
 * - Any active organization member can view terms.
 *
 * Possible responses:
 * - `200 OK`: Term retrieved successfully.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `403 Forbidden`: User is not an active member of the organization.
 * - `404 NotFound`: Term was not found in the organization.
 */
export const getTermByIdEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetTermByIdEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetTermByIdEndpointHandlerResponses, GetTermByIdEndpointHandlerErrors, ThrowOnError>({ url: '/api/terms/{id}', ...options });

/**
 * Get users (cursor pagination)
 *
 * Retrieves users using cursor-based pagination.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) and **Admin or SuperAdmin role** are required.
 *
 * **Query Parameters**:
 * - `cursor` (uuid, optional): Fetch users after this cursor (exclusive).
 * - `limit` (int, optional): Page size (default 20, max 100).
 * - `id` (uuid, optional): Filter by a specific user ID.
 * - `userName` (string, optional): Filter by username (partial match).
 *
 * The response includes:
 * - `Items`: Users for the current page.
 * - `NextCursor`: Cursor value for the next page (if any).
 * - `HasMore`: Indicates whether more users are available.
 *
 * Possible responses:
 * - `200 OK`: Users retrieved successfully.
 * - `403 Forbidden`: Current user does not have Admin or SuperAdmin privileges.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getUsersEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<GetUsersEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).get<GetUsersEndpointHandlerResponses, GetUsersEndpointHandlerErrors, ThrowOnError>({ url: '/api/users', ...options });

/**
 * Get current authenticated user
 *
 * Retrieves the profile of the currently authenticated user.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * The response includes:
 * - `Id`: User's unique identifier.
 * - `UserName`: Display username.
 * - `FirstName`, `MiddleName`, `LastName`: Personal names.
 * - `Role`: User's system role (e.g., Admin, User).
 *
 * Possible responses:
 * - `200 OK`: User profile successfully retrieved.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getCurrentUserEndpointHandler = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentUserEndpointHandlerData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentUserEndpointHandlerResponses, GetCurrentUserEndpointHandlerErrors, ThrowOnError>({ url: '/api/users/me', ...options });

/**
 * Update current user profile
 *
 * Updates the profile of the currently authenticated user.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Request Body**:
 * - `UserName` (string, optional): New display username.
 * - `FirstName` (string, optional): Updated first name.
 * - `MiddleName` (string, optional): Updated middle name.
 * - `LastName` (string, optional): Updated last name.
 * - `Email` (string, optional): Updated email address.
 * - `Password` (string, optional): New password (must meet complexity rules).
 *
 * **Notes**:
 * - Fields not included in the request body will remain unchanged.
 * - Attempting to set a username or email that already exists will result in a conflict.
 *
 * **Possible responses**:
 * - `200 OK`: Profile updated successfully.
 * - `400 BadRequest`: Invalid input or failed validation (e.g., invalid email format).
 * - `409 Conflict`: Username or email already in use.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const updateCurrentUserEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<UpdateCurrentUserEndpointHandlerData, ThrowOnError>) => (options.client ?? client).patch<UpdateCurrentUserEndpointHandlerResponses, UpdateCurrentUserEndpointHandlerErrors, ThrowOnError>({
    url: '/api/users/me',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get user by ID
 *
 * Retrieves the public profile of a user by their unique ID.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Route Parameters**:
 * - `id` (uuid): The unique identifier of the user to retrieve.
 *
 * The response includes:
 * - `Id`: User's unique identifier.
 * - `UserName`: Display username.
 * - `FirstName`, `MiddleName`, `LastName`: Personal names.
 * - `Role`: User's system role.
 *
 * Possible responses:
 * - `200 OK`: User profile successfully retrieved.
 * - `401 Unauthorized`: No valid session or session expired.
 * - `404 NotFound`: No user exists with the specified ID.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const getUserByIdEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<GetUserByIdEndpointHandlerData, ThrowOnError>) => (options.client ?? client).get<GetUserByIdEndpointHandlerResponses, GetUserByIdEndpointHandlerErrors, ThrowOnError>({ url: '/api/users/{id}', ...options });

/**
 * Update current user password
 *
 * Updates the password for the currently authenticated user.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) is needed.
 *
 * **Request Body**:
 * - `CurrentPassword` (string, required): The user's current password.
 * - `NewPassword` (string, required): New password (must meet complexity rules).
 * - `LogoutAll` (boolean, optional): If true, also revoke the current session (full logout).
 *
 * **Notes**:
 * - Other active sessions will be revoked after a successful password change.
 * - When `LogoutAll` is false (default), the current session remains active.
 *
 * **Possible responses**:
 * - `200 OK`: Password updated successfully.
 * - `400 BadRequest`: Invalid input or failed validation (e.g., password complexity).
 * - `401 Unauthorized`: Invalid session or current password is incorrect.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const updateCurrentUserPasswordEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<UpdateCurrentUserPasswordEndpointHandlerData, ThrowOnError>) => (options.client ?? client).patch<UpdateCurrentUserPasswordEndpointHandlerResponses, UpdateCurrentUserPasswordEndpointHandlerErrors, ThrowOnError>({
    url: '/api/users/me/password',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Promote user
 *
 * Promotes a user to the next role in the system hierarchy.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) and **SuperAdmin role** are required.
 *
 * **Request Body**:
 * - `UserId` (uuid, required): The unique identifier of the user to promote.
 *
 * **Role Hierarchy**:
 * - User → Admin → SuperAdmin
 * - Users already at the highest role (SuperAdmin) cannot be promoted.
 *
 * **Notes**:
 * - Only SuperAdmins can perform this action.
 * - Attempting to promote a non-existent user will return `404 NotFound`.
 * - Attempting to promote a user already at the highest role will return `409 Conflict`.
 *
 * **Possible responses**:
 * - `200 OK`: User successfully promoted.
 * - `403 Forbidden`: Current user does not have SuperAdmin privileges.
 * - `404 NotFound`: No user exists with the specified ID.
 * - `409 Conflict`: User is already at the highest role.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const promoteUserEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<PromoteUserEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<PromoteUserEndpointHandlerResponses, PromoteUserEndpointHandlerErrors, ThrowOnError>({ url: '/api/users/{id}/promote', ...options });

/**
 * Demote user
 *
 * Demotes a user to the previous role in the system hierarchy.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) and **SuperAdmin role** are required.
 *
 * **Request Body**:
 * - `UserId` (uuid, required): The unique identifier of the user to demote.
 *
 * **Role Hierarchy**:
 * - SuperAdmin → Admin → User
 * - Users already at the lowest role (User) cannot be demoted.
 *
 * **Notes**:
 * - Only SuperAdmins can perform this action.
 * - Attempting to demote a non-existent user will return `404 NotFound`.
 * - Attempting to demote a user already at the lowest role will return `409 Conflict`.
 *
 * **Possible responses**:
 * - `200 OK`: User successfully demoted.
 * - `403 Forbidden`: Current user does not have SuperAdmin privileges.
 * - `404 NotFound`: No user exists with the specified ID.
 * - `409 Conflict`: User is already at the lowest role.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const demoteUserEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<DemoteUserEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<DemoteUserEndpointHandlerResponses, DemoteUserEndpointHandlerErrors, ThrowOnError>({ url: '/api/users/{id}/demote', ...options });

/**
 * Lock user account
 *
 * Locks a user account and revokes all active sessions, preventing the user from logging in until unlocked.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) and **Admin or SuperAdmin role** are required.
 *
 * **Request Body**:
 * - `UserId` (uuid, required): The unique identifier of the user account to lock.
 *
 * **Notes**:
 * - Locked users cannot access the system until an Admin or SuperAdmin unlocks their account.
 * - Attempting to lock a non-existent user will return `404 NotFound`.
 * - Users who are already locked will still return `200 OK` (idempotent operation).
 *
 * **Possible responses**:
 * - `200 OK`: User account successfully locked.
 * - `403 Forbidden`: Current user does not have Admin or SuperAdmin privileges.
 * - `404 NotFound`: No user exists with the specified ID.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const lockUserEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<LockUserEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<LockUserEndpointHandlerResponses, LockUserEndpointHandlerErrors, ThrowOnError>({ url: '/api/users/{id}/lock', ...options });

/**
 * Unlock user account
 *
 * Unlocks a previously locked user account, allowing the user to log in again.
 *
 * **Authentication Required**: A valid session (`edu_session_id` cookie) and **Admin or SuperAdmin role** are required.
 *
 * **Request Body**:
 * - `UserId` (uuid, required): The unique identifier of the user account to unlock.
 *
 * **Notes**:
 * - Unlocking a user who is not currently locked will still return `200 OK` (idempotent operation).
 * - Attempting to unlock a non-existent user will return `404 NotFound`.
 *
 * **Possible responses**:
 * - `200 OK`: User account successfully unlocked.
 * - `403 Forbidden`: Current user does not have Admin or SuperAdmin privileges.
 * - `404 NotFound`: No user exists with the specified ID.
 * - `500 InternalServerError`: Unexpected server error.
 */
export const unlockUserEndpointHandler = <ThrowOnError extends boolean = false>(options: Options<UnlockUserEndpointHandlerData, ThrowOnError>) => (options.client ?? client).post<UnlockUserEndpointHandlerResponses, UnlockUserEndpointHandlerErrors, ThrowOnError>({ url: '/api/users/{id}/unlock', ...options });
