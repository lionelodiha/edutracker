// Dev placeholder (Stage 03). `npm run dev` serves this file as /config.js;
// the Docker entrypoint overwrites it at container start. Empty here means
// src/config.ts falls back to VITE_API_BASE_URL, then localhost.
window.__EDUTRACKER_CONFIG__ = {};
